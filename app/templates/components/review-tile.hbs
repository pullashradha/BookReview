<div class="review-tile row">
  <img src={{review.profilePicture}} alt={{review.username}} class="col-xs-4"/>
  <div class="col-xs-8">
    <h4>{{review.username}}</h4>
    <h4>Published On: {{review.date}}</h4>
    <h4>Rating: {{review-rating review.rating}}</h4>
    <p>
      {{review.reviewContent}}
    </p>
    <div>
      <h5>Favorite Books</h5>
      <ul>
        {{#each favoriteBooks.favoriteBooksList as |favoritedBook|}}
        <li>{{favoritedBook.title}} By {{favoritedBook.author}}</li>
        {{/each}}
      </ul>
    </div>
  </div>
</div>
